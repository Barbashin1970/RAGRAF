fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.12
httpx==0.27.2
rdflib==7.1.1
pyshacl==0.26.0
networkx==3.4.2
python-dotenv==1.0.1
duckdb>=1.2,<2

# OpenAI Python SDK — клиент для любого OpenAI-совместимого endpoint'а
# (Cerebras / Groq / Ollama / OpenAI). Все импорты внутри функций (lazy),
# поэтому без пакета бэкенд стартует, но /sandbox/chat сразу падает в
# mock с ModuleNotFoundError. На cloud-демо обязателен.
openai==2.36.0

# Document parsing для аналитика — загрузка PDF/DOCX как контекст в Q&A.
# Pure-Python, без системных зависимостей.
pypdf==5.1.0
python-docx==1.1.2

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0

# RAGU — heavy, optional. Раскомментируй когда подключаем GraphRAG-индекс.
# graph_ragu==0.1.*
